# About


Sample which reads the VS2026 external tools from a configuration file.

- Requires at least one external tool to be configured in VS2026.
- May need to adjust `Operations.FileName`

:bulb: Writes results to a json file and to the console.